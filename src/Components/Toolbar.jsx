import React from "react";

const Toolbar = () => {
  return (
    <div className="w-full bg-blue-100 text-blue-900 text-center text-xs md:text-sm py-2 border-b border-blue-200 shadow-sm font-medium tracking-wide">
      <span className="font-semibold">WARNING:</span> These products are not
      risk free and provide nicotine, which is addictive. Only for use by
      adults.
    </div>
  );
};

export default Toolbar;
